#pragma once

#include <cstdint>
#include <memory>
#include <optional>
#include <span>
#include <stdexcept>
#include <utility>
#include <vector>

typedef std::int32_t i32;
typedef std::uint32_t u32;
typedef std::size_t usize;

using std::nullopt;
using std::optional;
using std::pair;
using std::span;
using std::vector;
